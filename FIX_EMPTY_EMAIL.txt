🔧 WHY YOUR TEST EMAIL IS EMPTY (ALL ZEROS)
═══════════════════════════════════════════════════

The email system works perfectly!
But your database has NO DATA yet.

The earlier "Collect Data" button you clicked triggered a background job,
but we need to verify it completed and collected data.

═══════════════════════════════════════════════════
🚀 QUICK FIX - Run Data Collection on Railway
═══════════════════════════════════════════════════

Option 1: Use Railway Dashboard (Easiest)
──────────────────────────────────────────
1. Go to: https://railway.app
2. Open your project
3. Click on your service
4. Go to "Settings" tab
5. Scroll to "One-off Commands" or "Deploy" section
6. Run this command:

   npx tsx collect-data-now.ts

7. Wait 3-5 minutes
8. Watch the logs - you should see "✅ Data collection complete!"

Option 2: Use Railway CLI
──────────────────────────
If you have Railway CLI installed and linked:

   railway run npx tsx collect-data-now.ts

Option 3: Use the Employee Portal Button
─────────────────────────────────────────
1. Go to your Employee Portal
2. Find "Collect Data" button in System Controls
3. Click it
4. It will trigger a 30-day backfill
5. Wait 3-5 minutes
6. Check Railway logs to confirm completion

═══════════════════════════════════════════════════
✅ AFTER DATA COLLECTION COMPLETES
═══════════════════════════════════════════════════

1. Go back to Employee Portal
2. Click "Test Client Report" button again
3. Check your email

This time it should have:
✅ Real clicks/impressions numbers
✅ Charts with data
✅ Top pages & queries
✅ AI insights with actual analysis

═══════════════════════════════════════════════════
📊 WHAT'S HAPPENING
═══════════════════════════════════════════════════

Your email template is working perfectly (you received it!)
But it's pulling from an empty database.

The data collection script:
- Fetches last 30 days from Google Search Console
- Stores it in your PostgreSQL database
- Takes 3-5 minutes to complete
- Then emails will show real data

═══════════════════════════════════════════════════
🔍 HOW TO VERIFY DATA EXISTS
═══════════════════════════════════════════════════

In Railway dashboard, run this command to check:

   railway run npx tsx -e "import { PrismaClient } from '@prisma/client'; const p = new PrismaClient(); p.dailyMetric.count().then(c => console.log('Metrics:', c))"

Should show: "Metrics: [number greater than 0]"

═══════════════════════════════════════════════════
⚠️  IMPORTANT NOTES
═══════════════════════════════════════════════════

1. Google Search Console has 2-3 day delay
   - Data from last 2-3 days won't be available yet
   - This is normal!

2. First time setup requires manual data collection
   - After this, it runs automatically at 3 AM daily

3. The site showing in email is "www.stuchai.com"
   - If you want BerganCo data, ensure that site exists
   - Or the script picks the first active site

═══════════════════════════════════════════════════
🎯 TL;DR - DO THIS NOW
═══════════════════════════════════════════════════

1. Go to Railway dashboard
2. Run command: npx tsx collect-data-now.ts
3. Wait 5 minutes
4. Send test email again
5. Check inbox - should have data now!

═══════════════════════════════════════════════════
